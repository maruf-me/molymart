import ProductDetails from "@/components/product/ProductDetails";

const Details = () => {
  return (
    <div className="min-h-[calc(100vh-10rem)] _container relative mt-3">
      <ProductDetails />
    </div>
  );
};

export default Details;
